# Personal Portfolio — Design System

## 1. Foundation

### Desktop frame

| 항목 | 기준 |
| --- | --- |
| 기본 확인 화면 | **1920 × 1080px** |
| 좌우 화면 여백 | **각 30px** |
| 실제 콘텐츠 폭 | **1860px** (`1920 - 30 - 30`) |
| 브라우저 페이지의 기본 구성 | 소개 영역 520px + 간격 30px + 프로젝트 창 영역 1310px |
| 격자 배경 | 제공된 CSS `linear-gradient` 두 개, 간격 **21px** |

1860px는 1920px 뷰포트에서의 값이다. 작은 화면에서는 고정 너비로 잘라내지 않고 유연하게 줄인다. 1080px 높이는 주요 요소가 한 화면에 보이는 검토 기준이며, 콘텐츠가 늘면 자연스러운 세로 스크롤을 허용한다.

## 2. Spacing

기본 간격은 4px 단위(8, 12, 16, 24, 32, 40, 48px)를 우선한다. 화면의 **30px 바깥 여백**과 프로젝트 창의 **90px 가로 / 60px 세로** 계단 간격은 별도 고정 기준이다.

## 3. Color

| 역할 | 초기 값 | 용도 |
| --- | --- | --- |
| 배경 | `#fbfdfb` | 프로젝트 브라우저 |
| 기본 글자 | `#242323` | 제목과 설명 |
| 선 | 옅은 회색 | 배경 격자 |
| 비플레인 | `#62d91d` | 선택 점과 창 테두리 |
| 설화수 | `#ee7729` | 선택 점과 창 테두리 |
| hourtention | `#a875ff` | 선택 점과 창 테두리 |
| 자두야 | `#b43936` | 선택 점과 창 테두리 |

격자 선은 사용자가 제공한 `rgba(148, 163, 184, 0.2)`를 사용한다. 프로젝트별 색은 현재 디자인을 바탕으로 한 초기값이다. 원본 브랜드 가이드나 최종 Figma 값을 확인한 후 교체한다. 자두야 색은 기존 프로젝트 디자인 시스템의 주요 색을 참고했다.

## 4. Typography

- 기본: `Pretendard`, `SUIT`, 시스템 한글 산세리프 순서. 실제 폰트 파일은 아직 제공되지 않아 설치된 폰트에 따라 다르게 보일 수 있다.
- 현재 프로젝트 제목은 크고 짧게, 설명은 읽기 쉬운 줄 길이로 둔다.
- 프로젝트 창 제목은 탐색 버튼이므로 대비와 클릭 영역을 확보한다.
- 장식용 로고 문자와 실제 브랜드 로고는 별개다. 최종 에셋을 받으면 교체한다.

## 5. Components

### Monitor entry

메인 책상에는 원본을 그대로 복사한 `desk-temporary.png`를 사용한다. 이미지 속 모니터 화면 위치에 투명 링크를 올리고 Hover와 키보드 포커스 상태를 제공한다. 확정된 책상 비주얼이 나오면 이미지와 링크 위치를 함께 검토한다.

### Project selector

프로젝트마다 색 점과 이름을 제공한다. 선택한 항목에는 외곽 링과 이름을 항상 보여준다. Hover 및 키보드 포커스 때 다른 이름도 볼 수 있다.

### Project window

네 창이 제목 바를 남기며 겹친다. 선택한 창이 앞에 오고, 제목 바를 눌러 직접 선택할 수 있다. 실제 이미지는 프로젝트별 컴포넌트에 교체한다.

### Auto progress

6.5초 주기의 진행 막대를 표시한다. 창에 마우스가 있을 때와 키보드 포커스가 있을 때 진행을 멈춘다. 직접 선택 시 0부터 다시 시작한다.

## 6. Responsive behavior

- 1920px 기준 시안을 우선 검토한다.
- 1200px 아래에서는 창 간격과 소개 영역 비율을 줄인다.
- 760px 아래에서는 소개와 창을 세로로 쌓고, 프로젝트 이름을 항상 표시한다.
- 모바일 디자인은 자두야 앱의 402 × 874 기준을 복제하지 않는다. 이 사이트의 작은 화면 대응은 별도 후속 검토 대상이다.

## 7. Accessibility / QA

- 모든 주요 기능은 버튼 또는 링크로 구현한다.
- 키보드 포커스 표시를 유지한다.
- 자동 이동을 수동으로 멈출 수 있다.
- 사용자가 모션 감소를 설정했으면 자동 이동을 끈다.
- 대표 화면의 임시 시각물은 최종 이미지가 도착하면 대체 텍스트와 함께 교체한다.
