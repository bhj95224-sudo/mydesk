# Personal Portfolio — Product Requirements Document

## 1. Product overview

### 정의

작업자의 프로젝트와 시각적 스타일을 책상이라는 공간에서 소개하는 개인 웹 포트폴리오. 방문자는 메인 책상 화면의 모니터를 통해 프로젝트 브라우저로 이동한다.

### 작업 형태

- 개인 작업. 팀원 간 역할 분담이나 팀 승인 흐름은 없다.
- React + TypeScript + Vite 기반.
- GitHub 저장소를 이용해 다른 컴퓨터에서도 이어서 작업할 수 있는 구조.

## 2. 목표

방문자가 다음 질문에 빠르게 답할 수 있어야 한다.

1. 이 사이트가 누구의 어떤 작업을 보여주는가?
2. 프로젝트는 어디에서 볼 수 있는가?
3. 어떤 프로젝트가 있고, 현재 보고 있는 프로젝트는 무엇인가?
4. 자세한 사례를 어디에서 읽을 수 있는가? (상세 페이지는 다음 단계)

## 3. 대상 사용자

- 디자이너·개발자 채용 담당자 및 협업을 검토하는 사람.
- 짧은 시간에 대표 작업을 훑고, 관심 작업을 자세히 보고 싶은 사람.

## 4. 핵심 UX 원칙

**책상에서 발견 → 모니터로 진입 → 프로젝트 선택 → 상세 내용 확인**.

장식은 프로젝트 탐색을 방해하지 않아야 한다. 현재 선택된 프로젝트의 제목·설명·창이 같은 상태를 보여줘야 한다. 자동 전환은 사용자가 선택하거나 읽는 동안 방해하지 않아야 한다.

## 5. Information architecture

```text
포트폴리오
├── 메인 책상 (`#/`)
│   ├── 소개 문구
│   ├── 모니터 → 프로젝트 브라우저
│   └── 이미지 속 키보드 → 사용 도구/역량 (초기 버전은 시각 요소)
└── 프로젝트 브라우저 (`#/projects`)
    ├── 프로젝트 선택
    ├── 현재 프로젝트의 제목·요약
    ├── 겹친 프로젝트 창과 대표 화면
    └── 프로젝트 상세 페이지 (후속 작업)
```

프로젝트 목록 초안: `beplain`, `Sulwhasoo`, `hourtention`, `자두야`. 실제 공개 순서와 상세 내용은 확인 후 조정한다.

## 6. 화면 요구사항

### 6.1 메인 책상

- 프로젝트 브라우저와 다른 화면임이 분명해야 한다.
- 제공된 임시 책상 이미지의 모니터 화면이 클릭 가능한 진입점이어야 한다.
- 텍스트 링크로도 같은 화면에 들어갈 수 있어야 한다.
- 이미지 속 키보드는 현재 시각 요소이며, 도구/역량 상호작용은 아직 확정하지 않는다.

### 6.2 프로젝트 브라우저

- 모니터 클릭 후 처음 보이는 화면이다. 메인페이지가 아니다.
- 왼쪽에 현재 프로젝트의 제목·분류·간단한 설명·선택 버튼을 둔다.
- 오른쪽에 네 프로젝트 창을 일정한 간격으로 겹쳐 배치한다.
- 선택된 창은 맨 앞에 오고, 제목과 설명도 즉시 바뀐다.
- 왼쪽 버튼 또는 창 제목을 누르면 해당 프로젝트를 선택할 수 있다.
- 자동 전환은 초기값 6.5초로 설정한다. 직접 선택하면 타이머를 처음부터 다시 시작한다.
- 창에 마우스를 올리거나 키보드로 조작하는 동안 자동 전환을 멈춘다.
- 일시정지/재생 버튼과 다음 전환까지의 진행 상태를 보여준다.
- `prefers-reduced-motion` 환경에서는 자동 전환하지 않는다.
- 책상 화면으로 돌아가는 링크를 제공한다.

### 6.3 프로젝트 상세

현재는 요약 패널만 연결된 상태다. 프로젝트별 사례 페이지의 URL, 목차, 실제 이미지·링크는 사용자 결정 후 추가한다.

## 7. 콘텐츠 원칙

- 자두야의 앱 소개는 원본 PRD의 목적을 간단히 요약하되, **개인 기여라고 확인되지 않은 기능은 자신의 성과로 쓰지 않는다**.
- 비플레인 설명은 사용자가 보여준 최신 시안의 문장을 임시로 반영했다.
- 설화수와 hourtention에는 확정 정보가 없어 임시 문구를 표시한다.
- 연도, 역할, 성과 수치, 외부 링크는 확인 전까지 생성하지 않는다.

## 8. 성공 기준

- 1920 × 1080에서 좌우 30px의 여백이 지켜지고 모든 주요 요소가 1860px 콘텐츠 영역 안에 있다.
- 3초 안에 모니터가 프로젝트 진입점임을 알 수 있다.
- 현재 프로젝트를 제목·선택 버튼·앞 창으로 확인할 수 있다.
- 자동 전환이 직접 선택 직후 갑자기 실행되지 않는다.
- 키보드만으로 진입·선택·일시정지·복귀를 할 수 있다.
- `npm run typecheck`와 `npm run build`가 통과한다.
